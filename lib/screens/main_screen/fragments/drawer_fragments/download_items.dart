import 'package:flutter/material.dart';

class DrawerDownloadItemsFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: <Widget>[
        Center(
          child: Text("Downloaded Items"),
        )
      ],
    );
  }
}
