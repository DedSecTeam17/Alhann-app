import 'package:flutter/material.dart';

class DrawerLocalFilesFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: <Widget>[
        Center(
          child: Text("Local Files"),
        )
      ],
    );
  }
}
